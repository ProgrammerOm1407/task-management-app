// API configuration
const config = {
  // API base URL - use the Render-hosted API in production
  API_BASE_URL: 'https://task-management-app-7gk3.onrender.com/api',
  
  // Fallback to local development server if needed
  LOCAL_API_URL: 'http://localhost:5000/api'
};

export default config;